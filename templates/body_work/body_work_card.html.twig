<h3>{{ bodyWork.name }}</h3>
<p>{{ bodyWork.description }}</p>
<div>
    <h6>Avant</h6>
    <div>
        {% for imageBefore in bodyWork.befores %}
            <a class="fancybox" href="{{ asset(imageBefore.pathImage) }}" data-fancybox="before" /><img src="{{ asset(imageBefore.pathImage) }}" width="100"/></a>
        {% endfor %}
    </div>
</div>
<div>
<h6>Après</h6>
    <div>
        {% for imageAfter in bodyWork.afters %}
            <a class="fancybox" href="{{ asset(imageAfter.pathImage) }}" data-fancybox="after" /><img src="{{ asset(imageAfter.pathImage) }}" width="100"/></a>
        {% endfor %}
    </div>
</div>

